# 引き継ぎ資料 — voicevox-transcript-tool

最終更新: 2026-08-13  
作成者: Yusei Sakai (NAGARA, Inc.)  
リポジトリ: nagara-internal/voicevox-transcript-tool  
ローカルパス: ~/repository/voicevox-transcript-tool

---

## 1. システム概要

### 何をするツールか

**議事録テキスト → MP3音声ファイル変換ツール**。

`話者名: 発言内容` 形式のテキストファイルをVOICEVOX（日本語テキスト読み上げエンジン）に渡し、複数話者の音声を合成してMP3ファイルとして出力する。

### なぜ作ったか

マニュアル動画制作のための音声素材を生成するために開発。台本テキストをVOICEVOXキャラクター（ずんだもん、四国めたん等）に読み上げさせることで、動画のナレーション音声を効率的に作成できる。

**正式名称:** text-to-mp3 音声変換ツール（旧称: ながらかいご 音声変換ツール）

### 使用場面

1. マニュアル動画の台本テキストをMP3化
2. 複数話者が登場する会話形式のナレーション
3. VOICEVOX対応キャラクターを話者として割り当て

---

## 2. 技術スタック

| 項目 | 内容 |
|------|------|
| 言語 | Python 3.x |
| TTS エンジン | VOICEVOX Engine（Docker または アプリ） |
| 音声合成 | VOICEVOX HTTP API（http://localhost:50021） |
| 音声結合 | Python `wave` 標準ライブラリ + `numpy` |
| MP3変換 | `ffmpeg`（外部ツール、要インストール） |
| GUI | `customtkinter`（`gui_app.py`） |
| 設定 | `config.yaml` |

### 依存パッケージ（requirements.txt）

```
requests>=2.31.0      # VOICEVOX API呼び出し
pyyaml>=6.0           # config.yaml読み込み
tqdm>=4.65.0          # 進捗バー
customtkinter>=5.2.0  # GUIアプリ
pyinstaller>=6.0      # スタンドアロンアプリビルド（build_app.sh）
numpy                 # 無音WAV生成
```

### 外部依存

| ツール | 用途 | 入手方法 |
|--------|------|---------|
| VOICEVOX Engine | 音声合成 | Docker or 公式アプリ |
| ffmpeg | MP3変換 | `brew install ffmpeg` (macOS) |

---

## 3. ディレクトリ構造

```
voicevox-transcript-tool/
├── src/
│   ├── main.py            ← CLIエントリーポイント
│   ├── parser.py          ← 議事録テキストのパース
│   ├── synthesizer.py     ← VOICEVOX API呼び出し
│   ├── audio_combiner.py  ← WAV結合・MP3変換
│   ├── config_loader.py   ← config.yaml読み込み
│   └── gui_app.py         ← GUIアプリ（customtkinter）
├── config.yaml            ← 話者マッピング・音声設定
├── generate.sh            ← シェルラッパー（Docker自動起動付き）
├── requirements.txt
├── README.md
└── output/                ← 生成音声ファイルの出力先
    └── meeting_audio.mp3  ← デフォルト出力先
```

---

## 4. 入力テキストフォーマット

```
話者名: 発言内容
別の話者: 別の発言
# これはコメント行（スキップされる）
---
# セクション区切り（2秒の無音が挿入される）
継続行（コロンなし、前の話者の発言として扱う）
```

### パースルール（`src/parser.py`の実装）

| パターン | 挙動 |
|---------|------|
| `話者名: 発言内容` | 新しい発言として解析。コロンは半角・全角どちらも可（正規表現: `^(.+?)[:：](.+)$`） |
| `# コメント` | スキップ |
| `---` | セクション区切りエントリ（type: "section_break"）を挿入 |
| コロンなしの行 | 前の話者の発言の継続として追加 |
| 空行 | スキップ |

### テキストの自動分割

`synthesizer.py`の`synthesize_long_text()`により、**200文字を超えるテキストは句点（。）で自動分割**して個別に合成する。合成した音声は結合して1つのセグメントとして扱われる。

---

## 5. 設定ファイル（config.yaml）

```yaml
voicevox:
  base_url: "http://localhost:50021"  # VOICEVOXエンジンのURL
  timeout: 30                          # APIタイムアウト（秒）

speakers:
  山田太郎:
    speaker_id: 3       # ずんだもん
    speed_scale: 1.0
    pitch_scale: 0.0
  佐藤花子:
    speaker_id: 2       # 四国めたん
    speed_scale: 1.0
    pitch_scale: 0.0
  加藤二郎:
    speaker_id: 8       # 春日部つむぎ
    speed_scale: 1.0
    pitch_scale: 0.0
  # ← ここに話者を追加

default_speaker:
  speaker_id: 3         # 未定義の話者に使用されるデフォルト
  speed_scale: 1.0
  pitch_scale: 0.0

audio:
  silence_between_lines: 0.8      # 発言間の無音（秒）
  silence_between_sections: 2.0   # セクション区切りの無音（秒）
  sample_rate: 24000              # サンプルレート
  output_format: "mp3"            # "mp3" or "wav"
  mp3_quality: 2                  # ffmpegのqscale:a（2=高品質、5=標準）
```

### VOICEVOXキャラクターIDの主要一覧

`python src/main.py --list-speakers` で全リスト確認可能。主要なもの:

| ID | キャラクター | スタイル |
|----|------------|---------|
| 2 | 四国めたん | ノーマル |
| 3 | ずんだもん | ノーマル |
| 8 | 春日部つむぎ | ノーマル |
| 13 | 青山龍星 | ノーマル |
| 14 | 冥鳴ひまり | ノーマル |

---

## 6. 使い方

### 前提: VOICEVOXの起動

```bash
# Docker で起動（推奨）
docker run -d --rm --name voicevox -p 50021:50021 voicevox/voicevox_engine:cpu-latest

# 起動確認
curl http://localhost:50021/version
```

### 仮想環境のセットアップ

```bash
cd ~/repository/voicevox-transcript-tool

python -m venv venv
source venv/bin/activate    # macOS/Linux
pip install -r requirements.txt
```

### CLIの使い方

```bash
# ファイルから生成
python src/main.py transcript.txt

# テキスト直接指定
python src/main.py -t '施設長: こんにちは。今日はよろしくお願いします。'

# パイプから渡す
echo '施設長: こんにちは' | python src/main.py

# 出力先を指定
python src/main.py transcript.txt -o output/manual_audio.mp3

# カスタムconfig使用
python src/main.py transcript.txt -c my_config.yaml

# 話者一覧を表示
python src/main.py --list-speakers

# パース結果の確認（音声合成せず）
python src/main.py transcript.txt --dry-run
```

### generate.sh の使い方

`generate.sh`はCLIのラッパー。VOICEVOXが起動していない場合はDockerで自動起動する:

```bash
bash generate.sh transcript.txt
```

---

## 7. 内部実装詳細

### 7.1 CLIフロー（main.py）

```
1. 引数パース (argparse)
2. config.yaml読み込み (config_loader.py)
3. --dry-run の場合: パース結果を表示して終了
4. VOICEVOX接続確認 (synthesizer.check_connection())
5. --list-speakers の場合: 話者一覧を表示して終了
6. 入力取得 (ファイル / -t テキスト / stdin)
7. 音声合成 (synthesize_entries)
   - 各エントリをVOICEVOX APIに送信
   - section_breakはスルー（後で無音として処理）
   - 合成失敗はスキップ（warning表示）
8. WAV結合 (audio_combiner.combine_wav_segments)
9. MP3変換 (audio_combiner.convert_to_mp3) ← ffmpeg依存
10. 完了メッセージ（ファイルサイズ・時間表示）
```

### 7.2 VOICEVOX API呼び出し（synthesizer.py）

```python
# 2ステップAPI呼び出し（VOICEVOX仕様）
# Step 1: audio_query — テキストから音声クエリを生成
POST /audio_query?text={text}&speaker={speaker_id}
# → クエリJSON（speedScale, pitchScale等を上書き）

# Step 2: synthesis — クエリから音声合成
POST /synthesis?speaker={speaker_id}  body: query_json
# → WAVバイナリ

# リトライ: max 3回、間隔 1.0秒
```

### 7.3 音声結合（audio_combiner.py）

```
全セグメントのWAVフレームを結合:
- line セグメント: WAVフレーム + 発言間無音（0.8秒）
- section_break: セクション無音（2.0秒）

結合済みWAV → ffmpeg → MP3 (qscale:a=2)
ffmpegが見つからない場合はWAVで出力（警告表示）
```

### 7.4 無音生成（generate_silence）

`numpy.zeros`で無音サンプル配列を生成 → `wave`標準ライブラリでWAVバイナリに変換。サンプルレート: 24000Hz、16bit、モノラル。

---

## 8. GUIアプリ（gui_app.py）

`customtkinter`を使ったデスクトップGUIアプリが実装されている。

スタンドアロンアプリとしてビルドするスクリプト（`build_app.sh`）も存在し、`pyinstaller`でパッケージング可能。

GUIの詳細な仕様はソースコード(`src/gui_app.py`)を参照。CLIと同じ処理を呼び出している。

---

## 9. --dry-run の出力例

```
📝 パース結果:

  [  1] ✅ 施設長 (ID:3): こんにちは。今日はよろしくお願いします。
  [  2] ✅ スタッフ (ID:2): はい、よろしくお願いします。
  [  3] --- セクション区切り ---
  [  4] ⚠️ 未定義話者 (ID:3): ...

📊 合計: 4 エントリ
   話者: 3 名 (スタッフ, 施設長, 未定義話者)

⚠ 以下の話者はconfig.yamlにマッピングがありません（デフォルト話者を使用）:
   - 未定義話者
```

`✅` = config.yamlに話者マッピングあり  
`⚠️` = マッピングなし（default_speakerを使用）

---

## 10. エラー・トラブルシューティング

### VOICEVOXが起動していない

```
❌ VOICEVOXが起動していません。
   接続先: http://localhost:50021
   起動方法: docker run -d --rm --name voicevox -p 50021:50021 voicevox/voicevox_engine:cpu-latest
```

→ Dockerコマンドを実行してから再試行

### ffmpegが見つからない

```
⚠ ffmpegが見つかりません。WAV形式のまま出力します。
  MP3に変換するにはffmpegをインストールしてください:
  macOS: brew install ffmpeg
```

→ `brew install ffmpeg` してから再試行。WAVで問題なければそのまま使える（config.yamlで`output_format: "wav"`に変更）

### 合成が部分的に失敗する

```
⚠ 合成失敗: 3 行（スキップ済み）
```

→ `--dry-run`で問題行を確認。テキストに特殊文字が含まれていないか確認

### Dockerがない環境でVOICEVOXを使う

VOICEVOXの公式アプリ（Windows/Mac/Linux向けGUIアプリ）をインストールして起動するとDockerなしで`http://localhost:50021`が使える。

---

## 11. 新しい話者を追加する方法

`config.yaml`の`speakers`セクションに追加:

```yaml
speakers:
  # 既存の話者...
  ナレーター:
    speaker_id: 13     # VOICEVOX話者ID（--list-speakersで確認）
    speed_scale: 0.9   # 速度（0.5〜2.0）
    pitch_scale: 0.05  # ピッチ（-0.15〜0.15）
```

台本テキスト側では`ナレーター: 発言内容`と書けば使える。

---

## 12. 今後の改善案

### 優先度高

1. **GPU対応**: `voicevox/voicevox_engine:nvidia-latest`（CUDA対応）に切り替えると合成速度が大幅改善。CPU版は長い台本で時間がかかる
2. **エラー時の自動リトライ設定**: 現在`max_retries=3`、`retry_interval=1.0`がコード内ハードコード。config.yamlで設定可能にする

### 優先度中

3. **話者IDの自動解決**: config.yamlに`speaker_id`数値でなく`speaker_name: "ずんだもん"`で指定できるようにする
4. **台本プレビュー**: `--dry-run`の出力をGUIに統合
5. **バッチ処理**: 複数の台本ファイルを一括処理

### 優先度低

6. **SSML対応**: 強調、ポーズ等の細かい読み上げ制御
7. **出力ファイル名の自動命名**: タイムスタンプや台本ファイル名を使った命名

---

## 13. 社員向け導入ガイド（GitHub Pages）

### 概要

非エンジニア社員向けのチュートリアルサイトをGitHub Pagesで公開している。

| 項目 | 内容 |
|------|------|
| URL | https://nagara-internal.github.io/voicevox-transcript-tool/ |
| ソース | `docs/` フォルダ（mainブランチ） |
| 公開設定 | GitHub Pages → Source: main / /docs |
| ファイル構成 | `docs/index.html`、`docs/style.css`、`docs/images/` |

### サイト構成

```
docs/
├── index.html          ← チュートリアル本体（全4ステップ）
├── style.css           ← スタイル
└── images/
    ├── icon.png                      ← favicon / OGPアイコン（512×512px）
    ├── voicevox_01_homepage.png      ← VOICEVOX公式サイトのスクショ
    ├── voicevox_02_download_dialog.png  ← Intel版ダウンロード設定
    └── voicevox_03_download_apple.png   ← Apple Silicon版ダウンロード設定
```

### チュートリアルの構成

| STEP | 内容 | 補足 |
|------|------|------|
| STEP 1 | ツールのZIPダウンロード | ダウンロードボタン直リンク |
| STEP 2 | setup.commandの実行 | Sequoia Gatekeeper対応手順あり |
| STEP 3 | VOICEVOXのインストール | Intel/Apple切り替えタブUI付き |
| STEP 4 | 毎日の使い方 | デスクトップのランチャー起動 |

### macOS Sequoia（15+）Gatekeeper対応

macOS 15以降では右クリック→「開く」が使えないため、両STEPに専用手順を追加済み。

**対象ファイル:** `setup.command`、`VOICEVOX.app`

**手順（共通）:**
1. ダイアログの「完了」をクリック
2. システム設定 → プライバシーとセキュリティ
3. 「〇〇はブロックされました」の横の「このまま開く」をクリック
4. パスワード入力 → 「開く」をクリック

### スクリーンショットの撮り方（更新時）

```bash
# CGWindowIDでChrome特定ウィンドウをキャプチャ
screencapture -l <CGWindowID> /tmp/raw.png

# Pillowでサイドバー・影を除去
python3 -c "
from PIL import Image
img = Image.open('/tmp/raw.png')
w, h = img.size
img.crop((548, 55, w-20, h-20)).save('docs/images/voicevox_XX.png')
"
```

### アイコン生成プロンプト（再生成時の参考）

```
A cute, simple app icon for a Japanese voice conversion tool.
Style: rounded square (iOS app icon shape), dark navy blue background (#0f3460).
Main character: a friendly green ghost or blob mascot wearing headphones.
Elements: speech bubble with "..." on the left, musical notes / sound wave bars on the right.
Color palette: navy background, bright green (#4ade80) accent.
Clean flat illustration style, no text.
512x512px.
```

---

## 14. マニュアル動画での使用方法

マニュアル動画台本音声化のフロー:

```
1. マニュアル台本をテキストファイルで作成
   → 「施設長: xxxxx」「スタッフ: xxxxx」形式で書く

2. config.yamlの speakers に台本の話者名を追加
   → VOICEVOXキャラクターIDを割り当て

3. 生成
   bash generate.sh transcript.txt -o output/manual_chapter1.mp3

4. 生成されたMP3を動画編集ソフト（CapCut、iMovieなど）に取り込む

5. 映像と合わせてマニュアル動画を完成
```

関連プロジェクト: `~/ai-management/projects/nagara-kaigo-tutorial-video/status.md`
